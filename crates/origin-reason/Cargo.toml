[package]
name = "origin-reason"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
origin-core = { path = "../origin-core" }
origin-logic = { path = "../origin-logic" }
origin-evidence = { path = "../origin-evidence" }
