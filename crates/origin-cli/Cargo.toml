[package]
name = "origin-cli"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
origin-core = { path = "../origin-core" }
origin-store = { path = "../origin-store" }
origin-evidence = { path = "../origin-evidence" }
origin-causal = { path = "../origin-causal" }
origin-fast = { path = "../origin-fast" }
origin-runtime = { path = "../origin-runtime" }
origin-profiler = { path = "../origin-profiler" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
