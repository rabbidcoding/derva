[package]
name = "origin-verify"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
origin-core = { path = "../origin-core" }
origin-kernel = { path = "../origin-kernel" }

[dev-dependencies]
origin-evidence = { path = "../origin-evidence" }
origin-logic = { path = "../origin-logic" }
origin-constraints = { path = "../origin-constraints" }
