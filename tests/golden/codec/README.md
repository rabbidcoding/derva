# ORIGIN-Ω ZERO — Codec Golden Vectors & Malleability Fixtures

> **INVARIANT:** 100% byte-for-byte cross-platform determinism.

- `canonical_vectors.bin`: Golden binary vectors representing valid canonical encodings.
- `malleable_vectors.bin`: Non-canonical, overlong, or padded byte streams designed to test decoder rejection.
